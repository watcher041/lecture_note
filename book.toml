[book]
title = "物理学入門"
authors = ["watcher041"]
language = "ja"
src = "src"

[output.html]
additional-css = ["option.css"]

[preprocessor.katex]
after = ["links"]

# cargo install mdbook 
# github pagesのため、docs にビルド結果を出力
# mdbook serve --dest-dir docs
# 数式は以下のように記載する
# ブロック数式：\$ ～ \$
# インライン数式：$ ～ $
# ~/.cargo/configに追記
# [net]
# git-fetch-with-cli = true
